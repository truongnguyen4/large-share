<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="12dp"
    android:paddingBottom="4dp">

    <AutoCompleteTextView
        android:id="@+id/actv_dialog_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Search device id…"
        android:textSize="14sp"
        android:completionThreshold="1"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:maxLines="1"/>

    <TextView
        android:id="@+id/tv_dialog_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tick the peers you want to display"
        android:textSize="11sp"
        android:textColor="#888888"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_dialog_peers"
        android:layout_width="match_parent"
        android:layout_height="320dp"/>

    <TextView
        android:id="@+id/tv_dialog_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No peers match your search."
        android:textSize="13sp"
        android:textColor="#999999"
        android:textStyle="italic"
        android:gravity="center"
        android:padding="16dp"
        android:visibility="gone"/>
</LinearLayout>
